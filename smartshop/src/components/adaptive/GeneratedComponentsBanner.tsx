"use client";

/**
 * LLM generation status is intentionally not shown to participants.
 * Generation still runs in the background; Adaptive Engine UI remains the source of truth.
 */
export function GeneratedComponentsBanner() {
  return null;
}
